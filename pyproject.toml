[project]
name = "0921"
version = "0.1.0"
description = "Autonomous Snake AI powered by Laya non-autoregressive decision model on ModernBERT-large with BFS anti-trap pathfinding and real-time cyber Web UI"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "laya>=0.3.3",
    "uvicorn>=0.53.0",
    "websockets>=17.1",
]
